<div id="tabs">
  <ul>
    <li><%= link_to t('.patients'), :patients, :class => "active" %></li>
    <li><%= link_to t('.requisitions'), :accessions %></li>
  </ul>
</div>

<% flash.each do |name, msg| %>
  <div id="flash" class="flash_notice">
    <%=h msg %>
  </div>
<% end %>
