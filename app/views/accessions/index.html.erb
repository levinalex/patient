<% title "Accessions" %>

<%= render @patient if @patient %>

<div class="accessions">
  <table>
    <tr>
      <th>Accession #</th>
      <th>Date of Extraction</th>
      <th>Reported Date</th>
      <th>Note</th>
    </tr>
    <%= render @accessions %>
  </table>
</div>

<%= link_to "New Accession", new_patient_accession_path(@patient) if @patient %>